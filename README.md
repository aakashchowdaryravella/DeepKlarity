# 🧠 DeepKlarity AI Quiz Generator (Gemini + Flask)

This project is a lightweight **AI-powered text generation app** built with **Python (Flask)** and **Google’s Gemini API**.  
It allows users to enter a prompt (like a topic or text snippet) and receive generated output (e.g., quiz questions, explanations, or summaries) using the Gemini large language model.

---

## 🚀 Features
- 🔹 Generate text dynamically from user prompts
- 🔹 Flask backend with API endpoints for `/api/generate` and `/api/list-models`
- 🔹 Simple, responsive frontend built with HTML + JavaScript
- 🔹 Environment-based configuration via `.env` file
- 🔹 Compatible with Google Generative AI (Gemini) library `google-generativeai==0.8.5`

---

## 🧩 Tech Stack

| Component | Technology |
|------------|-------------|
| **Backend** | Flask (Python) |
| **Frontend** | HTML, CSS, Vanilla JS |
| **AI Model** | Google Gemini API (`google-generativeai`) |
| **Environment** | `.env` using `python-dotenv` |
| **Hosting** | Localhost / any Flask-compatible cloud |

---

## 🗂️ Project Structure
